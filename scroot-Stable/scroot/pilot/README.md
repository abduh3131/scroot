# scroot/pilot

The pilot package aggregates Abdallah's autonomy loop (perception, navigation,
and control) behind a simple Python API.  Scripts can import `scroot.pilot` to
spin up the `AutonomyPilot`, parse CLI arguments, or run the headless CLI loop
without touching GUI internals.
